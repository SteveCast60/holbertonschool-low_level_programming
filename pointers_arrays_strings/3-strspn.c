#include <stdio.h>

/**
 * _strspn - Entry point
 * @s: input
 * @accept: input
 * Return: always 0 (success)
 */

unsigned int _strspn(char *s, char *accept)
{
	char s = accept;
	int cont;
	int result;

	while (*s != '\0')
	{
		result = (cont + 1);
	}
	return (result);
}
